# LIH Web App

## Dev Setup
If running `npm run dev` make sure to set the proxy in `package.json` to the API gateway

## Running the server

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```
