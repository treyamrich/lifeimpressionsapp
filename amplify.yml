version: 1
# backend:
#   phases:
#     build:
#       commands:
#         - '# Execute Amplify CLI with the helper script'
#         - amplifyPush --simple
#
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - NEXT_PUBLIC_BUILD_ENV=${NEXT_PUBLIC_BUILD_ENV}
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
